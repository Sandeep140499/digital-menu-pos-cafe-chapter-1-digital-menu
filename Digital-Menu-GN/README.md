# Frontend (Vite + React)

This package is the customer menu and admin/employee dashboards. **Project overview, environment variables, and deployment (Railway, Render, AWS EC2/RDS, Vercel)** are documented in the **[repository root README.md](../README.md)**.

```bash
npm install
npm run dev
```

Production build: `npm run build` → static output in `dist/`.
